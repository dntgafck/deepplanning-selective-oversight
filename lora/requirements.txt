# Pinned versions for reproducibility. Generated alongside setup_pod.sh.
# These pins represent the resolved state on a working H100 + CUDA 13 + Python 3.11 pod
# as of May 2026.
#
# To recreate this exact env after a successful pod run:
#   pip freeze > requirements.lock.txt
# Then commit requirements.lock.txt and use that for downstream pods.

# Torch stack (install from --index-url https://download.pytorch.org/whl/cu130)
torch==2.11.0
torchvision
torchaudio

# Training framework
transformers>=4.50
peft>=0.14
trl>=0.15
accelerate>=1.0
datasets>=3.0
bitsandbytes>=0.45

# HF utilities
huggingface-hub>=0.30
hf-transfer>=0.1

# Observability
wandb>=0.18

# Small utility deps
einops
ninja               # for flash-attn build

# OPTIONAL: flash-attn. Install via:
#   pip install flash-attn --no-build-isolation
# (Must be installed AFTER torch because it builds against torch's ABI.)
